apiVersion: v1
kind: Service
metadata:
  name: user-svc
  labels:
    app: user-service
spec:
  selector:
    app: user-service
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
  type: ClusterIP

---

apiVersion: apps/v1
kind: Deployment

metadata:
  name: user-service
  labels:
    app: user-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
        - name: user-service
          image: caschuen/cnd-joboffers-user-service
          envFrom:
            - configMapRef:
                name: job-offers-config-map
          ports:
            - containerPort: 8080
      restartPolicy: Always
