package thi.cnd.authservice.test.core;

import org.springframework.boot.test.context.TestConfiguration;

public class JwtProviderUnitTests {

    // @TestConfiguration (valid.yaml) / (invalid.yaml)
    // JwtConfig Unit Tests: Test constructor parameters, test if generated keys are ok

    // createAccountAccessToken
    //  > Mock JWT Keys
    //  > Mock JWT Config

    // createClientAccessToken
}
